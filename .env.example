NEXT_PUBLIC_API_URL=https://fadeai-platform-production.up.railway.app

# Basic-auth gate for /admin/* (see src/middleware.ts)
ADMIN_USER=
ADMIN_PASS=

# Bearer token for /api/admin/audit and admin mutators on the Railway backend
CRON_SECRET=

# Header token for /api/admin/cappers/* on the Railway backend
ADMIN_API_TOKEN=
